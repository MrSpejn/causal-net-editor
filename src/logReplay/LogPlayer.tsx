import ProcessLog from "../logLoaders/ProcessLog";
import GraphVisualization from '../graphVisualization/GraphVisualization';

class LogPlayer {
    replay(percentile: number, log: ProcessLog, graphviz: GraphVisualization) {
    }
}

export default LogPlayer